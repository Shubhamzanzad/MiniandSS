#include "constants.h"
void prof_login_handler(int desc) {
    printf("%s", LOGIN_ID);
    int id;
    scanf("%d", &id);
    printf("%s", PASSWORD);
    char password[50];
    scanf("%s", password);

}